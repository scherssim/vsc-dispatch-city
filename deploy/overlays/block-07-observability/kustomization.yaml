apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../block-06-persistence
  - cluster-observer.yaml
  - rabbitmq-plugins.yaml
  - monitoring.yaml
  - grafana-dashboard.yaml
patches:
  - path: observability-patches.yaml
labels:
  - pairs:
      course.teko.ch/block: "07"
    includeSelectors: false
